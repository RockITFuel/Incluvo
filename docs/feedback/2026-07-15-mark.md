# Feedback Mark Timmermans (Ondivera) — 15-07-2026

Bron: e-mail "Eerste bevindingen Incluvo" + bijlage "1. de Ondivera Vragenlijst
Incluvo.docx". Geanalyseerd op 16-07-2026 (13 codebase-onderzoeken, per punt).
Context: Mark is ±27 juli t/m 14 augustus op vakantie en wil daarna uitgebreid
testen — doel is zoveel mogelijk van sectie A vóór die test af te ronden.

Legenda effort: **S** = klein (uren) · **M** = middel (dag/dagen) · **L** = groot (week+).

---

## A. Direct oppakken — geen input van Mark nodig

Oorzaak is gevonden en de oplossing is eenduidig; alle benodigde bouwstenen
bestaan al in de codebase.

### A1. Bugfixes (wave 1 — eerst doen)

| # | Item | Bevinding (root cause) | Fix | Effort |
|---|------|------------------------|-----|--------|
| 1 | Oude deadline blijft staan op welkomstpagina | "Verplaats naar vandaag" zet alleen `pinnedForToday`; de `nextDeadline`-memo (`welkom/index.tsx:230`) sorteert puur op `dueAt` en ziet gepinde taken niet | Gepinde taken meenemen als "vandaag" in de memo + `task.changed` SSE-subscription toevoegen (taken-pagina's hebben die al) | S |
| 2 | Leerling ziet schooltemplate onder Cursussen | Zichtbaarheidsfilter in `courses.list` (`courses/index.ts:311`) heeft een `return true`-fallthrough voor same-tenant templates; zelfde gat in `loadReadable` | Leerling-branch beperken tot eigen `student_execution`; spiegelen in `loadReadable`; regressietest | S |
| 3 | Coach kan antwoorden leerling niet meer bekijken | **Regressie.** `latestPlan` (`dashboard/index.ts:233`) pakt nieuwste submission op `updatedAt` zónder statusfilter → na indienen + nieuwe lege draft linkt "Open coachplan" naar de lege draft | Nieuwste non-draft submission verkiezen (zelfde statuslijst als inbox), draft alleen als fallback; overige `orderBy(updatedAt).limit(1)`-plekken auditen | S |
| 4 | Geen notificatie bij nieuw chatbericht (coach) | Alles bestaat al (`chat_message`-enum, bel, NL-labels) behalve dat `chat.send` nooit `notify()` aanroept — alleen het SSE-event voor een open thread | `notify()` per ontvanger in `send` (±15 regels) + klik op notificatie deep-linkt naar `/chat?conversationId=…` | S |
| 5 | AI-advies gebruikt coachformulier niet | Klopt: prompt bevat alleen naam + leervoorkeur-labels + huidig vraaglabel; nooit echte antwoorden. `/assistent`-tab stuurt zelfs níets mee | Context server-side opbouwen in de `assistant`-procedure uit `formAnswer`/`formQuestion`/mappings bij `submissionId` (laadcode bestaat al) | S/M |
| 6 | 'Sla over' / 'Bespreken met coach' "werken niet" | Statisch volledig correct aangesloten, incl. coach-aandachtindicator. Vermoedelijk UX: geen succes-feedback en "Sla over" springt direct door | Live naspelen in browser (runtime-bug uitsluiten); dan: opgeslagen-bevestiging + overgeslagen/bespreken-badge op de vraagstap zelf | S |

### A2. Features waarvan de bouwstenen al bestaan (wave 2)

| # | Item | Bevinding | Plan | Effort |
|---|------|-----------|------|--------|
| 7 | Vertalen voor leerling | Verrassing: vertalen is volledig gebouwd (server + UI) maar zit achter coach-only `/assistent`; server staat alle ingelogde gebruikers al toe | Translate-UI herbruikbaar maken en ontsluiten voor de leerling (a11y-paneel of knop bij content) | S |
| 8 | Hardop voorlezen | Pure stub: switch zet een dood store-veld; nergens `speechSynthesis` (ook niet in clp) | Bouwen op native browser SpeechSynthesis (`nl-NL`), voorlees-knop per contentvlak, stop/pauze | M |
| 9 | Transcriptietool "niet volledig" | Pipeline (opname → Groq Whisper → voorstellen → Overnemen) wérkt live. Echte gaten: transcripten niet terug te openen (geen list/get) en "Importeer Teams/Zoom" accepteert alleen `audio/*` terwijl Teams/Zoom `.mp4` exporteren | Read-endpoint + panel laadt laatste transcript bij mount; accept-filter verbreden; lege-invoer-validatie; timeout voor lange gesprekken | M |
| 10 | Plusknop + paperclip in chat | Beide letterlijk decoratief (`aria-hidden`, geen onClick). `message.attachmentStorageKey` bestaat al; upload-stack van cursussen is herbruikbaar | Plus → bestaande nieuwe-chat-picker; paperclip → `"chat"` storage-scope + presign/confirm-procedures + weergave in thread | M |
| 11 | Taak aanmaken: uitschuifpaneel + profielpagina | Precies waar Mark het verwachtte; mutatie (`tasks.add`) bestaat, alleen knop+modal ontbreken (= backlog #43) | "Taak toevoegen"-snelactie in quickpanel + "+ Taak" op profielpagina, zelfde modal | S |
| 12 | Taak aanmaken vanuit chat | `message.taskId` zit al in het schema en de chat-UI bevat een slapende taakkaart-renderer (= backlog #7, destijds bewust uitgesteld) | Transactionele `chat.createTask` (task + message in één), taakkolommen in `chat.messages`-select, composer-knop | M |
| 13 | Taak koppelen aan cursusonderdeel | `task` kent alleen `assignmentId`; geen generieke link naar sectie/contentblok | Nullable `contentBlockId` op `task` + picker in de taakmodals van #11/#12 | S/M |
| 14 | Auto-taak "vragenlijst klaarstaat" bij indienen | Nu alleen een bel-notificatie; `task` is leerling-only, dus coach-taak kan niet | `assigneeCoachId` (nullable) op `task` + taak aanmaken in `submit()` naast bestaande notify | S |
| 15 | Formulierenmanager: mapping- en optie-editor | Schema/server ondersteunen `mapsToQuestionId` + `options` volledig; beheer-UI kan ze niet bewerken (mappings kunnen nu alleen via seed-script) | "Koppel aan coachvraag"-dropdown + keuzes/schaal-editor in `plan/beheer` | M |
| 16 | Vragenlijst-bijlage invoeren | Grotendeels **al gedaan**: de echte Ondivera-vragenlijst incl. leerling↔coach-mappings zit in `seed-coachplan.ts`; stappen 2/3/5 van de happy flow (coach vult in met transcriptie, antwoorden naast coachvragen, ouders-akkoord) werken al | Seed diffen tegen de nieuwe docx: dikgedrukte toelichtingen per POPP-vraag achter een info-icoon/uitklap + verwijzingen OCW/Kennisnet/Steunpunt | S |
| 17 | Ondivera-logo gebruiken | Branding is inline SVG op 3 plekken (app-shell, public-layout, login) + ontbrekende favicon | SVG van ondivera.nl halen en Brand-componenten + favicon vervangen | S |

---

## B. Eerst input van Mark nodig

Bouwen kan hier pas na een productbeslissing — verkeerd gokken betekent
schema-migraties terugdraaien. Voorstel: deze vragen vóór zijn vakantie mailen,
uitwerken in de gezamenlijke sessie erna.

| # | Item | Wat we van Mark willen weten | Waarom blokkerend |
|---|------|------------------------------|-------------------|
| 18 | Voortgangsmodel (aanbevolen + gekozen content) | (a) Mag een leerling zélf content toevoegen/afmelden voor de voortgang, of moet de coach dat (mee)bevestigen? (b) Wat gebeurt met eerdere keuzes als de leervoorkeuren later wijzigen? (c) Blijft er een harde bouwer-vlag "telt nooit mee" (bijv. intro-pagina's)? | Bepaalt het schema van de nieuwe per-leerling-keuzetabel en of er een goedkeurings-flow (notificatie + ack) bij moet. Herstructurering voortgangsberekening (gedeelde helper voor cursus + dashboard) kan wél alvast. Effort M–L |
| 19 | Groepen leerlingen | (a) Is een groep privé van de coach of schoolbreed zichtbaar? (b) Afleiden is nu een losgekoppelde kopie: template-wijzigingen komen níet bij al toegewezen leerlingen terecht — acceptabel, of moet groepstoewijzing live gekoppeld zijn? | Kopie-vs-link bepaalt de hele architectuur van groepstoewijzing. Groepstabellen + multi-select derive kunnen daarna snel. Effort M |
| 20 | Afspraken (taak = afspraak) | (a) Volstaat v1 met een vaste meeting-link in het coachprofiel + doorsturen (wachtkamer van Teams/Zoom/Meet), zónder agenda-API-integratie? (b) Is een downloadbare agenda-uitnodiging (.ics) genoeg voor "in zijn agenda zetten"? | Statische link = L; echte Teams/Zoom/Meet- of agenda-integratie = veel groter + extern beheer. Coachprofiel heeft bovendien nog geen enkele bewerk-UI — die moet er sowieso bij |
| 21 | Statusindicator coach (groen/oranje/rood) | Wat telt als "actief" (drempel naar oranje)? En wat is precies "in gesprek met een andere leerling" (open chatvenster? actieve call?) | Groen/oranje is bijna gratis via de bestaande SSE-hub; rood vereist een definitie die er nog niet is. Kan als MVP groen/grijs starten zodra (a) beantwoord is |
| 22 | Versiebeheer vragenlijst | (a) Gaan bij "leerling hoeft niet opnieuw in te vullen" ook de overslaan/bespreken-vlaggen en leervoorkeuren mee? (b) Blijft "ouders akkoord" staan over versies heen, of per versie opnieuw? (c) Keuze per nieuwe versie of als template-standaard? | Bepaalt kopieerlogica en waar het akkoord-veld komt te wonen (submission vs. leerling-niveau). Schema-basis (`versionNumber`, `supersedesId`) hangt hiervan af |
| 23 | Leervoorkeuren-vocabulaire | Mark levert "definitieve labels binnenkort". Ook: mogen scholen het vocabulaire uitbreiden of is het strikt Ondivera-breed? | De app-brede vocabulairetabel (vervangt hardcoded lijst + freeform strings) willen we in één keer met de definitieve labels migreren |
| 24 | AI-trainingsmateriaal | Mark levert nog aan | Gaat in de bestaande kennisdocument-RAG-pipeline; tot die tijd niets te doen |
| 25 | Toestemming opnames (ouders) | Reeds open in `docs/QUESTIONS-FOR-MARK.md` (H4): moet transcriptie geblokkeerd worden zonder vastgelegde toestemming? | Zonder antwoord bouwen we hooguit een waarschuwingsbanner in het transcriptiepaneel (kan alvast, zie #9) |
| 26 | "Afleiden" doorpraten | Geen vraag maar agendapunt (Mark wil het architectuurverhaal horen): afleiden = diepe kopie mét blijvende `parentCourseId`-afstamming (Ondivera-template → schooltemplate → leerling-uitvoering), atomair; gewoon kopiëren zou die afstamming verbreken. Wijzigingen stromen ná afleiden niet door — raakt direct aan #19 | Gespreksstof voor de sessie na zijn vakantie |

---

## Voorgestelde volgorde

1. **Wave 1** (vóór Marks vakantie): A1 volledig — zes fixes, allemaal S, allemaal direct zichtbaar voor Mark.
2. **Wave 2**: A2 — #16 (docx-diff) en #11/#14 eerst (raken zijn happy flow), dan 7/9/10/12/13/15/17, #8 als laatste (enige zonder bestaande bouwstenen).
3. **Vragenmail naar Mark** (deze week): B18–B22 + B25, zodat de sessie na zijn vakantie meteen knopen kan doorhakken.
4. **Wave 3** (na de sessie): voortgangsmodel, groepen, afspraken, versiebeheer, vocabulaire-migratie.
