node_modules
dist
.output
.turbo
.vinxi
.tanstack

# env
.env
.env.local
.env.*.local

# generated route tree — committed (like clp) so CI type-check has it; the
# Vite build regenerates it. See apps/web/vite.config.ts (TanStack Start plugin).

# local-dev course uploads (S3 fallback)
apps/server/uploads

# kennisdocumenten source texts — third-party reference material (UDL paper,
# manuscript). Kept local, NOT committed (copyright). Regenerate from the source
# attachments; see apps/server/seed-data/kennisdocumenten/README.md. manifest.json
# IS tracked.
apps/server/seed-data/kennisdocumenten/*.txt

# logs
*.log
npm-debug.log*

# os / editor
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json
